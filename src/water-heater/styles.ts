import { css } from 'lit';

export const styles = css`
    .footer-status-row {
        display: flex;
        gap: 8px;
        margin-right: auto;
    }

    .settings-divider {
        height: 4px;
    }
`;
