import { css } from 'lit';

export const styles = css`
    .sub-row { display: flex; align-items: center; font-size: 0.8rem; color: var(--secondary-text-color); }
    .dot { width: 6px; height: 6px; border-radius: 50%; margin-right: 6px; }
    .sub-lbl { flex-grow: 1; }
    .sub-val { font-weight: 600; color: var(--secondary-text-color); }
`;
